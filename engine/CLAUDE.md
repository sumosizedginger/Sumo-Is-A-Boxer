# CLAUDE.md

Read `AGENTS.md` first.

Then read `DOCUMENTATION_MAP.md` and load only the canonical documents required for the active task.

Canonical repository:

`sumosizedginger/My-Game-Engine-1.0`

This is a greenfield implementation repository with an already-designed architecture.

Do not treat donor repositories as canonical implementation.

Do not duplicate project policy in this file.

If this file conflicts with a canonical project document, the canonical document wins.

For material work, return the handoff required by `HANDOFF_PROTOCOL.md`.

Never self-certify implementation as final acceptance.
